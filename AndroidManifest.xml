<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="net.hssco.club">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="smartpos.deviceservice.permission.Printer" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <application
        android:allowBackup="false"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:theme="@style/Theme.SajedPOS"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity android:name=".SplashActivity" android:theme="@style/AppTheme.FullScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".PaymentResultActivity"
            android:theme="@style/Theme.SajedPOS" />
        <activity
            android:name=".CustomerPinActivity"
            android:theme="@style/Theme.SajedPOS" />
        <activity
            android:name=".AdminPinActivity"
            android:theme="@style/Theme.SajedPOS" />
        <activity
            android:name=".EditCustomerActivity"
            android:theme="@style/Theme.SajedPOS" />


        <activity
            android:name=".AdminMenuActivity"
            android:theme="@style/Theme.SajedPOS" />

        <activity
            android:name=".ChangeAdminPinActivity"
            android:theme="@style/Theme.SajedPOS" />

        <activity
            android:name=".MerchantMenuActivity"
            android:theme="@style/Theme.SajedPOS" />

        <activity android:name=".WelcomeActivity" android:theme="@style/AppTheme.FullScreen"/>
        <activity android:name=".SelectOperationActivity" android:theme="@style/AppTheme.FullScreen" />
        <activity android:name=".AmountActivity" android:theme="@style/AppTheme.FullScreen" />


    </application>

</manifest>
